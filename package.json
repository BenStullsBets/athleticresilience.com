{
  "name": "athleticresilience-site",
  "version": "1.0.0",
  "private": true,
  "description": "athleticresilience.com — static marketing site (Eleventy)",
  "scripts": {
    "build": "eleventy && node scripts/fingerprint.js",
    "serve": "eleventy --serve",
    "deploy": "npm run build && wrangler deploy",
    "preview": "npm run build && wrangler dev"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "wrangler": "^4.104.0"
  }
}
